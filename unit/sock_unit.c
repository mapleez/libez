#include "ez_sock.h"

int main (int argc, char* argv []) {
	// char* ptr = ez_addr_to_string (,);
	// printf ("%s\n", ptr);
	int ipaddr = 0;
	int res = 0;
	// unsigned char* ptr = '\0';
	res = ez_stringaddr_to_int (&ipaddr, "....");

	// if (ptr != '\0');
	return 0;
}
